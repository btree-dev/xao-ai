# Node.js dependencies
node_modules/

# Build outputs
dist/
build/
coverage/
.tmp/
.temp/
packages/**/dist/
apps/**/dist/
apps/**/build/

# Hardhat & contract build artifacts (retain source under contracts/)
packages/contracts/artifacts/
packages/contracts/cache/
packages/contracts/typechain-types/

# Vite cache
**/.vite

# Cypress / Playwright test artifacts (if added later)
cypress/videos/
cypress/screenshots/
playwright-report/
test-results/

# TypeScript incremental build info
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files
.env
.env.*
!.env.example

# OS / Editor junk
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json
!.vscode/launch.json

# Misc caches
.cache/
.next/
.vercel/
.eslintcache

# Local test reports
reports/

# Runtime data
*.pid
*.pid.lock

# Coverage / instrumentation
nyc_output/

# Type checking artifacts
# (if using tsc --emitDeclarationOnly maybe ensure dist/ is ignored already)

# Generated types
**/*.d.ts
!src/**/*.d.ts  # Keep any hand-maintained declaration files inside src if needed (adjust as required)
!packages/**/src/**/*.d.ts

# Lock files (commit these for deterministic installs)
# If you use npm this will be package-lock.json
# If you use yarn it will be yarn.lock
# If you use pnpm it will be pnpm-lock.yaml
# All are intentionally NOT ignored.
# (Remove specific lines below to ignore one, but that is discouraged.)
# yarn.lock
# pnpm-lock.yaml
package-lock.json

# Local ENS / chain snapshots
*.chainstate
*.tenderly

# Local wallet keystore exports
keystore-*.json

# MacOS metadata
.Spotlight-V100
.Trashes

# JetBrains shelf
.idea/shelf/

# VSCode workspace storage
.vscode/.userdata/

# Yarn 2 zero-installs (if ever used) - comment out to commit
.yarn/cache/
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Temporary scratch files
*.local.*

# Firebase / other service configs
serviceAccount*.json

# Git merge / rebase artifacts
*.orig
